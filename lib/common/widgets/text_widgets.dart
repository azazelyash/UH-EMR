import 'package:flutter/material.dart';

class TextWidgets {
  TextWidgets._();

  Text headingText({required String text}) {
    return Text(
      text,
      style: const TextStyle(),
    );
  }
}
